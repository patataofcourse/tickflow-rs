use super::TickflowOp;

pub enum MegamixOp {
    Call(i32),
    CallSub(u32),
    CallFunc(u32),
    SetFunc { func: u32, pos: i32 },
    KillAll,
    KillCat(u32),
    KillLoc(i32),
    KillSub(u32),
    RunSub(u32),
    RunFunc(u32),
    Run(i32),
    Return,
    Stop,
    Cat(u32),
    SetCondvar(i32),
    AddCondvar(i32),
    PushCondvar,
    PopCondvar,
    Rest(u32),
    SetRest { slot: u32, amount: u32 },
    GetRest(u32),
    Delay(u32),
    RestReset,
    Unrest(u32),
    Other(TickflowOp),
}
